#"Hello" "world"
#Concatenating the texts
# We can use +(plus) sign or we can use ,(comma)
print("Hello" + "World")
#HelloWorld
#plus sign will concatenate the string(text) as is .

print("Hello","World")
# comma(,) will concatenate the string(text) and
# will put a space between them

